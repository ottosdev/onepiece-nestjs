import { Test, TestingModule } from '@nestjs/testing';
import { PersonagesController } from './personages.controller';

describe('PersonagesController', () => {
  it('should be defined', () => {
    expect('controller').toBeDefined();
  });
});
